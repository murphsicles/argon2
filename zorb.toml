[package]
name = "@crypto/argon2"
version = "0.1.0"
edition = "2026"
description = "Memory-hard password hashing (Argon2id) for Zeta — pure Zeta implementation per RFC 9106"
license = "MIT OR Apache-2.0"
repository = "https://github.com/murphsicles/argon2"
keywords = ["argon2", "password-hashing", "kdf", "cryptography"]
categories = ["cryptography"]

[dependencies]
@crypto/sha2 = ">=0.1.0"
